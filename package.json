{
  "name": "herdr-wiff",
  "version": "0.1.0",
  "description": "A herdr plugin that opens wiff code-review sessions in a pane and routes human comments to the agent that owns the diff.",
  "license": "MIT",
  "//": "private: installs come from GitHub via `herdr plugin install`, never npm.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "smol-toml": "^1.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.10"
  }
}
